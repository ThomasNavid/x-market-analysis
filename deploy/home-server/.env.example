# Copy this file to `.env` (same folder) and set a real password. NEVER commit `.env`.
# This password is what the Mac uses in its DATABASE_URL, so make it strong —
# e.g. generate one with: openssl rand -base64 24
POSTGRES_PASSWORD=change-me-strong-password
