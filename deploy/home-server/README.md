# Home Server (Windows PC)

Docker Compose stack for the home-server PostgreSQL database (`findb`), plus the
config you copy to the Windows PC. Ollama runs as a native Windows app, not in Docker.

Full step-by-step setup and operations runbook: [`documentation/home-server.md`](../../documentation/home-server.md)
